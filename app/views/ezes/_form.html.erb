<%= form_for(@eze) do |f| %>
  <% if @eze.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@eze.errors.count, "error") %> prohibited this eze from being saved:</h2>

      <ul>
      <% @eze.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
