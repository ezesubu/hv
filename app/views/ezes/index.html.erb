<h1>Listing ezes</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @ezes.each do |eze| %>
      <tr>
        <td><%= eze.name %></td>
        <td><%= link_to 'Show', eze %></td>
        <td><%= link_to 'Edit', edit_eze_path(eze) %></td>
        <td><%= link_to 'Destroy', eze, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Eze', new_eze_path %>
